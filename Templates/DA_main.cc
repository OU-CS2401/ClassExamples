#include "DAcontainer.h"
#include <string>

using namespace std;

// Main program. Usually this would be in its own .cc file
int main(){
    // instantiation for int
    DArray<int> nums;

    // instantiation for string
    DArray<string> names;
}